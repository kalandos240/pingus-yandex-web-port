# Active Pingus WebAssembly build

This branch exists for the current reproducible build iteration of the original Pingus 0.7.6 browser port.

The workflow must compile the original source and either persist the next compiler/linker diagnostic in `ci/` or publish the validated `release/pingus-yandex-web.zip` package.
